diagram test_impossible {
  width: 800
  height: 600
  
  # Too many elements, impossible layout
  for i in range(20):
    x = range(5, i+ 5
    for y = i * 4
    
    x += 20
    y += 10
    x += 5
    x += 5
  }
}
